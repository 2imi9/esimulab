services:
  genesis-sim:
    build:
      context: .
      dockerfile: docker/genesis-sim/Dockerfile
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ./data:/app/data
      - ./scripts:/app/scripts:ro
      - ./src:/app/src:ro
    env_file:
      - path: .env
        required: false
    profiles:
      - gpu

  gaussiancity:
    build:
      context: .
      dockerfile: docker/gaussiancity/Dockerfile
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    volumes:
      - ./data:/app/data
      - ./models/gaussiancity:/models:ro
    env_file:
      - path: .env
        required: false
    profiles:
      - city

  web-viewer:
    build:
      context: .
      dockerfile: docker/web-viewer/Dockerfile
    ports:
      - "${WEB_PORT:-8000}:8000"
    volumes:
      - ./data:/app/data:ro
    env_file:
      - path: .env
        required: false
    depends_on:
      genesis-sim:
        condition: service_completed_successfully
        required: false
